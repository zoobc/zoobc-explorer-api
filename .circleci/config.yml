version: 2
jobs:
  deploy:
    docker:
      - image: circleci/node:8.10
    working_directory: ~/zoobc-explorer-api
    steps:
      - add_ssh_keys:
          fingerprints:
            - "17:49:86:b6:a6:8b:f0:eb:7b:53:3a:3a:c1:40:5b:33"
      - checkout
      - run:
          name: Setup SSH Key Host
          command: |
            chmod 755 .circleci/deploy.sh
            ssh-keyscan -H 139.162.15.80 >> ~/.ssh/known_hosts
      - run:
          name: Install Dependencies
          command: npm ci
      - run:
          name: Deploy
          command: ./.circleci/deploy.sh
      # - run:
      #     name: Fix SSH could not resolve hostname
      #     command: ssh-keyscan -H 139.162.15.80 >> ~/.ssh/known_hosts
      # - run:
      #     name: Deploy Over SSH
      #     command: |
      #       ssh root@139.162.15.80 "ls -ltr"
      # - add_ssh_keys
          # fingerprints:
          #   - "95:c3:f9:41:99:ba:43:26:75:1d:dc:23:bb:58:c4:6d"
      # - run:
      #     name: Add SSH key to known hosts
      #     command: |
            # ssh-keyscan -H 139.162.15.80 >> ~/.ssh/known_hosts
            # ssh root@139.162.15.80 pwd
      # - run:
      #     name: Deploy Over SSH
      #     command: |
      #       ssh root@139.162.15.80 "ls -ltr"
      # - checkout
      # - run:
      #     name: Open SSL RSA
      #     command: |
      #       ssh-keyscan -H 139.162.15.80 >> ~/.ssh/known_hosts
      #       ssh root@139.162.15.80 pwd
      # - run:
      #     name: Install Node Modules
      #     command: |
      #       npm install
      #       # npm start
      # - run:
      #     name: Update Schema Sub-module
      #     command: |
      #       git submodule init
      #       git submodule update --remote
      # - restore_cache:
      #     keys:
      #       - v1-dependencies-{{ checksum "package.json" }}
      #       - v1-dependencies-
      # - save_cache:
      #     paths:
      #       - node_modules
      #     key: v1-dependencies-{{ checksum "package.json" }}

workflows:
  version: 2
  build:
    jobs:
      - deploy
